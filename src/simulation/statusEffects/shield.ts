// Shield absorption is handled by StatusEffectManager.consumeShield() — no tick or multiplier here.
import type { StatusEffectHandler } from './types';

export const shieldHandler: StatusEffectHandler = {};
